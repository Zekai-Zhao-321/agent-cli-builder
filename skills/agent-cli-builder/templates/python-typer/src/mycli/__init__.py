"""mycli — an agent-native command-line interface.

Replace this docstring and the rest of the package with your own CLI; the
scaffold here implements the agent-first invariants (output contract, error
envelope, input hardening, async tasks, schema introspection) and a working
`hello` command end-to-end.
"""

__version__ = "0.1.0"
